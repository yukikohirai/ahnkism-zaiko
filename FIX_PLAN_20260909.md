# 修正プラン 2026-09-09（Fable作成 → Opus実装用）

対象リポジトリ: `/Users/apple/Documents/Codex/2026-08-17/new-chat/work/hpb-inventory`
本番: https://ahnkism-zaiko.vercel.app（GitHub `yukikohirai/ahnkism-zaiko` main への push で自動デプロイ）
Supabase project ref: `tcxjqflprokxewpdoqfw`（MCPの `execute_sql` / `apply_migration` が使える）

## 実装者への絶対ルール
- **憶測で実装しない。** 不明点はユーザー（平井さん・非エンジニア）に日本語で質問する。
- **「やりました」報告は必ず証拠付き。** DBの件数・合計値のクエリ結果、`npm run build` の成功、
  GitHub deployments API の `success` を確認してから報告する。
- 9月分の入力データ（`inventory_movements` 等）は**絶対に消さない・書き換えない**。
- DELETE文は実行しない（必要なら平井さんに Supabase SQL Editor で実行してもらう）。
- パスワードには一切触れない。

## 前提となる現状（Fable調査済み）
- `store_products(store_id, product_id, opening_stock, required_qty, sort_order, is_active, dealer_override)`
  `opening_stock` = 8月末の繰越在庫。現在庫 = `opening_stock + Σ inventory_movements.quantity`（`src/app/admin/page.tsx` `HqOverview.currentStock`）。
- stores: 1=LABO, 2=nit, 3=elu
- categories: 1 カラー剤, 2 ユーショー, 3 パーマ・スパ・その他, 4 トリートメント, 5 備品, 6 店販, 7 店販②, 8 店販オージュア
- 店舗入力画面（`src/app/[storeId]/input/page.tsx`）は `categories` 全件 + その店舗の `store_products` から商品を出す。
  → 新カテゴリを作って `store_products` に3店舗分の行を入れれば、3店舗の入力画面に自動で出る。
- 本部画面 `HqOverview` のタブ: `selectedView === 'retail'`（「全店 店販・Aujua」＝ 店販系カテゴリの店舗別在庫を横並び）と 各店舗タブ。
  「全店在庫」タブは**存在しない**。
- スプシ `/Users/apple/Downloads/【堀江本店】2026 ８月　在庫管理表.xlsx` の `全店在庫` シート:
  列 A=メーカー(結合セル) B=商品名 C=必要数(全部空) D=在庫数(8月末) F=前月繰越 G=各店使用数, 商品1行＋店舗3行(本店/nit/心斎橋)の4行組。
  ユーザー回答: **eluのシートは不要、LABOシートが親。**

---

## タスク① 全店在庫タブ（新規）

### 1-A. データ投入（SQLマイグレーション `supabase/migrations/20260909_shared_stock.sql` を作り、`apply_migration` で適用）
1. `categories` に `(id=9, name='全店在庫', sort_order=9)` を追加（id は明示的に9で良い。既存最大は8）。
2. `products` に下記29件を insert。`category_id=9`, `is_active=true`, `brand=null`, `manufacturer`=下表のメーカー, `dealer=null`, `required_qty=0`, `sort_order`=1..29（表の順）。
   `product_code/purchase_route/image_path/import_key` は null。
3. `store_products` に 29商品 × 3店舗 = 87行 insert。
   - `store_id=1(LABO)`: `opening_stock`=下表の在庫数, `required_qty=0`
   - `store_id=2,3`: `opening_stock=0`, `required_qty=0`
   - 全行 `is_active=true`, `sort_order`=1..29（店舗ごとに同じ順）
   - ※ `モノ` はシートが -1 なので 0 にする（8月取り込み時の「マイナスは0に補正」と同じ方針）。報告時に明記。

| # | メーカー(manufacturer) | 商品名(name) | LABO opening_stock |
|---|---|---|---|
| 1 | (null) | アマテラス6.0銀 | 2 |
| 2 | (null) | アマテラス9.0緑 | 0 |
| 3 | (null) | ルヴニール シャンプー | 8 |
| 4 | (null) | ラポール00 | 8 |
| 5 | (null) | ラポール01 | 8 |
| 6 | (null) | ラポール02 | 8 |
| 7 | センス | 95SENSE シャンプー500ml | 19 |
| 8 | センス | 95SENSE トリートメント500g | 25 |
| 9 | センス | 95SENSE シャンプー詰替 | 0 |
| 10 | センス | 95SENSE トリートメント詰替 | 7 |
| 11 | きくや（ムコタ） | AHNKISMオイル SS | 5 |
| 12 | きくや（ムコタ） | AHNKISMオイル EM | 4 |
| 13 | きくや（ムコタ） | AHNKISMオイル EMレフィル | 0 |
| 14 | きくや（ムコタ） | AHNKISMオイル 緑 | 2 |
| 15 | リファ | ReFa ストレートアイロンプロ | 1 |
| 16 | リファ | ReFa カールアイロンプロ32 | 0 |
| 17 | リファ | ReFa カールアイロンプロ38 | 8 |
| 18 | リファ | パワーストレート（ワイドアイロン） | 0 |
| 19 | リファ | ドライヤー W | 5 |
| 20 | リファ | ドライヤー スマートS＋ | 0 |
| 21 | リファ | ドライヤー SE | 1 |
| 22 | (null) | スピカ | 2 |
| 23 | (null) | モノ | 0 (シートは-1) |
| 24 | (null) | ツガミ | 0 |
| 25 | (null) | リュクス ケラチンブースター250g | 0 |
| 26 | (null) | Cインナーボンド | 0 |
| 27 | (null) | シチリン ピリン | 0 |
| 28 | (null) | Dルネート | 0 |
| 29 | (null) | Fルネート | 0 |

  検証クエリ（報告に結果を貼る）:
  `select count(*) from products where category_id=9;` → 29
  `select store_id, count(*), sum(opening_stock) from store_products sp join products p on p.id=sp.product_id where p.category_id=9 group by 1;` → 1:29/113, 2:29/0, 3:29/0

### 1-B. 本部画面に「全店在庫」タブ（`src/app/admin/page.tsx` の `HqOverview`）
- タブボタンを追加: `selectedView === 'shared'`、ラベル「全店在庫」。位置は「全店 店販・Aujua」の右。
- 対象行: `assignments` のうち `product.category_id` が「全店在庫」カテゴリ（`categories.find(c => c.name === '全店在庫')` で id を引く。ハードコード9は避ける）。
- 商品ごとに1行。列（店舗別在庫の表と同じ見た目・同じ Tailwind クラスを流用）:
  `商品（manufacturer 小字 + name）| 繰越 | LABO | nit | elu | 入荷 | 現在庫 | 必要数`
  - 繰越 = LABO行の `opening_stock`
  - LABO/nit/elu = その店舗の今月 `inventory_movements` のうち **マイナスの合計を正の数で**（使用・販売）。0 は空表示。
  - 入荷 = 3店舗のプラス合計（purchase_order/transfer_in など）
  - 現在庫 = 繰越 + Σ(3店舗すべての movements.quantity)  ← これが「繰越 − 打ち込み分」
  - 必要数 = LABO行の `required_qty`。既存の `saveStoreRequired` を LABO行に対して使い、タップ編集できるようにする。
- `movements` の取得範囲を確認すること: 現状 `loadOverview` が取っている `inventory_movements` の期間・件数制限（`.limit` や当月フィルタ）を読んで、全店在庫29商品×3店舗が漏れないようにする。
- **各店舗タブと「全店 店販・Aujua」タブからは全店在庫カテゴリを除外**する（nit/elu は opening 0 なので現在庫がマイナス表示になって混乱するため）。
- 検索ボックスは全店在庫タブでも効かせる。

### 1-C. 店舗入力画面
- コード変更は原則不要のはず（カテゴリ＋store_products で自動表示）。
  実際に `https://ahnkism-zaiko.vercel.app/1/input` `/2/input` `/3/input` を Playwright で開き、カテゴリ「全店在庫」と29商品が出ることを確認して報告（ログインは honbu アカウント。パスワードは平井さんに入力してもらう）。

---

## タスク② シルク系（LABO 店販 att 10商品）の繰越を修正

原因: 8月末取り込み時、店販シートで att の10行は「必要数(D列)が空欄」で、在庫数(E列)が取り込まれず 0 になっていた。
ユーザー回答: **在庫数を入れて、必要数は 0 のまま。**
9月の入力（product 630 に入荷+18、635 に使用-2）は残っているので `opening_stock` を直せば現在庫は自動で正しくなる。

`store_id=1` の `store_products.opening_stock` を以下に UPDATE（`updated_at=now()` も）:

| product_id | brand/name | opening_stock |
|---|---|---|
| 634 | att シルクシャンプー | 15 |
| 636 | att シルクトリートメント | 18 |
| 633 | att シルクオイル | 27 |
| 638 | att シルクミスト | 20 |
| 632 | att アンプル | 20 |
| 630 | att L | 17 |
| 639 | att ツガミ | 20 |
| 631 | att THE ONE シャンプー | 17 |
| 635 | att シルクシャンプー詰替 | 25 |
| 637 | att シルクトリートメント詰替 | 44 |

（シート E列「在庫数」= 繰越35 + 入 − 出 の8月末値。行72〜91を Fable が目視確認済み）

検証: `select product_id, opening_stock from store_products where store_id=1 and product_id between 630 and 639 order by 1;` の結果を報告に貼る。
本部画面 LABO タブで「シルクシャンプー詰替」が 25−2=23、「L」が 17+18=35 と表示されることを確認。

elu のシルク: ユーザー回答「LABOのシートだけで足りる」→ **elu側は触らない。**

---

## デプロイ手順
1. `npm run build` が通ること
2. `git add -A && git commit -m "..." && git push origin main`
3. `gh api repos/yukikohirai/ahnkism-zaiko/deployments` → 最新の `statuses_url` を見て `success` になったのを確認（`sleep` は使わず、数回ポーリング）
4. 報告: 変更内容 / 検証クエリ結果 / デプロイURL。**確認していないことは「確認していない」と書く。**

---
---

# 第2弾（2026-09-09 追加）タスク③〜⑥

ユーザー回答済み。以下はすべて確定仕様。不明点が出たら実装せず質問を持ち帰ること。

## タスク③ 在庫がマイナスになる打ち込みを許可する（本部・入出庫画面）
現状: `supabase/migrations/20260821_inventory_movements.sql` の
`record_inventory_operation`（出庫時 `在庫が不足しています` で raise）と
`record_stock_transfer`（移動元の在庫チェックで raise）が、在庫が0を下回る登録を拒否している。
店舗入力（`complete_inventory_session`）にはこのチェックが無く、本部だけ拒否される。

やること:
1. 新マイグレーション `supabase/migrations/20260909_allow_negative_stock.sql` で上記2関数を
   `create or replace` し直し、**在庫不足の raise だけを削除**（他のロジック・引数・権限は元のまま）。
   `p_quantity <= 0` のチェックは残す（数量はプラス入力、符号は種類で決まる仕様は変えない）。
   `apply_migration` で適用し、`select pg_get_functiondef('public.record_inventory_operation'::regproc)` に
   「在庫が不足」が含まれないことを確認して報告に貼る。
2. 本部画面（`src/app/admin/page.tsx` HqOverview の店舗別在庫・全店在庫タブ、入出庫画面の在庫表示）で
   現在庫がマイナスのとき赤字（`text-red-600`）にする。
3. 入出庫画面 `src/app/admin/operations/page.tsx` のエラー表示文言はそのまま（RPCが raise しなくなるだけ）。

## タスク④ 入出庫画面に「誤差調整」モードを追加（実在庫を入れる方式）
場所: `src/app/admin/operations/page.tsx`。既存の mode（入荷 / 減少 / 店舗移動）と並べて **「誤差調整」** を追加。
UI:
- 店舗・日付・商品検索は既存と同じ。
- 数量欄の代わりに **「実際の在庫数」** 入力（0以上の整数）。選択中カードに「今の在庫 X → 実在庫 Y（差分 ±Z）」をリアルタイム表示。
- 差分0なら登録ボタンを無効化。
- 登録時は `record_inventory_operation` を `p_movement_type='adjustment'`, `p_quantity=|差分|` で呼ぶ … **ただし現行RPCは adjustment を常にプラス扱い**（`when p_movement_type in ('purchase_order','adjustment') then p_quantity`）。
  → タスク③のマイグレーションで `record_inventory_operation` を直す際に、**新しい引数を足さず**、`adjustment` のときは `p_quantity` の符号をそのまま採用（マイナスも許可）するよう変更する。
  具体的に: `p_quantity <= 0` チェックを `if p_movement_type <> 'adjustment' and p_quantity <= 0 then raise` に変え、
  `v_signed_quantity` を `adjustment` のとき `p_quantity` そのまま、それ以外は従来どおり。`adjustment` で `p_quantity = 0` は raise。
  `p_note` に `'誤差調整 実在庫 Y（前 X）'` を入れる。
- 履歴（月別入出庫履歴の `MovementCell`・入出庫画面の直近一覧）では既存の `adjustment` 表示（「調」/在庫調整）をそのまま使う。ラベルは「在庫調整」→「誤差調整」に統一（`MOVEMENT_META.adjustment.label` と `MOVEMENT_LABELS.adjustment`）。

## タスク⑤ 月別入出庫履歴（`src/app/admin/page.tsx` InventoryHistoryTable）の合計列
現状: 右端に「月計」1列のみ。商品名（左固定）と月計（右端）が離れている。
やること（列順を変更）:
`ブランド | 商品名 | 店舗 | 月計 | 業務 | 店販 | 個人 | 1日 … 月末`
- 月計〜個人の4列も `sticky` で左に固定（ブランド/商品名/店舗の右に連続して固定。`left-*` の値は列幅の合計で正しく計算し、背景色をつけて重なりを隠す）。
- 業務 = `movement_type='usage'` の合計、店販 = `retail_sale`、個人 = `personal_sale`（いずれも出庫なので正の数で表示、0は「−」）。
- 月計は現状どおり全種類の符号付き合計。
- 表幅が増える分、スマホでは横スクロールになる。固定列の合計幅が画面幅（375px）を超えないよう、列幅は
  ブランド w-14 / 商品名 w-28 / 店舗 w-10 / 月計・小計 各 w-9 程度に詰める（実装後に `npm run build` 後の CSS ではなく、Playwright で `/admin` を375px幅で開いて崩れないか確認したいが、ログイン不可なら「未確認」と報告）。

## タスク⑥ 店舗入力画面の「最近使った商品を上に表示」を停止
`src/app/[storeId]/input/page.tsx`:
- `displayedProducts` の `.sort(...)`（usageRank による並び替え）を削除し、常に `store_products.sort_order` の順（= 取得時の順）で表示。
- 「最近入力した商品を上に表示しています」の文言を削除。
- `recentUsage` / `usageRank` が他で使われていなければ state・fetch ごと削除（使われていれば残す。判断根拠を報告）。

## 共通
- 9月の入力データは触らない。DELETE禁止。パスワード非接触。
- `npm run build` → commit → push → GitHub deployments が success になるまで確認 → 証拠付きで報告。

---
---

# 第3弾（2026-09-09 追加）タスク⑦〜⑨
ユーザー回答済みの確定仕様。第2弾（③〜⑥）が main に入った後に着手すること（同じファイルを触るため、必ず `git pull` してから）。

## タスク⑦ 入出庫画面の商品一覧を全件・店舗入力と同じ順に（`src/app/admin/operations/page.tsx`）
現状: `availableProducts` が ブランド名順 → `.slice(0, 80)`、さらに「減少」モードは店販系カテゴリのみ。
やること:
- `.slice(0, 80)` を撤去し、**全モード（入荷／減少／店舗移動／誤差調整）で対象店舗の取扱商品を全件**出す。減少モードの `retailCategoryIds` 絞り込みも撤去。
- 並び: **カテゴリ `sort_order` → その店舗の `store_products.sort_order`**（店舗入力画面と同じ）。`store_products` の select に `sort_order` を追加する。
- 一覧内にカテゴリ見出し行（`bg-slate-50 text-xs font-bold`）を入れる。検索中も見出しは残す（該当なしのカテゴリは出さない）。
- 一覧の枠は既存の `max-h-64 overflow-y-auto` を `max-h-[60dvh]` に広げる（テーブル内スクロール）。
- 店舗移動モードの「移動先にも登録済みの商品だけ」フィルタは残す。

## タスク⑧ 商品管理：各行に「取扱店舗」を出し、ON/OFF できるように（`src/app/admin/products/page.tsx`）
- 一覧の各行（編集中でないとき）に LABO / nit / elu の3つのチップを表示。取扱中＝青、未取扱＝グレー。タップで切替。
  - ONにする: `store_products` に行が無ければ insert（`is_active=true, required_qty=0, opening_stock=0, sort_order` = その店舗・そのカテゴリの最大 `sort_order`+1、無ければ1）。行があれば `is_active=true` に update。
  - OFFにする: `store_products.is_active=false` に update（**削除しない**。在庫・履歴は残る）。取扱店舗が0になる操作は `confirm` で確認。
- データ読込: 一覧表示用に `store_products.select('store_id, product_id, is_active')` を全件取得して Map 化。
- 「＋ 新しい商品を追加」フォームは現状のまま（本当に新しい商品用）。フォーム上部に一文「他店舗で取扱中の商品をこの店舗にも置きたい場合は、一覧の店舗チップをタップ」を添える。

## タスク⑨ 発注先を「商品ごとに1つ」に統一し、店舗別の上書きを廃止
ユーザー決定: 店舗別にばらけていた27件はすべて **商品側の値** に統一（A ウェルム22件＝ウェルム、B 566＝きくや、C 612＝みつい、D 438＝レユ、E 562＝アクティム）。
やること:
1. マイグレーション `supabase/migrations/20260909_drop_dealer_override.sql`:
   `update public.store_products set dealer_override = null where dealer_override is not null;`（453行）。列自体は残してよい（削除しない）。
   適用後 `select count(*) from store_products where dealer_override is not null;` → 0 を報告に貼る。
2. `src/app/admin/page.tsx` `supplierName()` を `row.product.dealer || row.product.manufacturer || '発注先未設定'` に変更（`dealer_override` を参照しない）。型 `StoreProductSummary.dealer_override` と select 句からも外す。他に `dealer_override` を参照している箇所があれば同様に外す（`grep -rn dealer_override src` で確認して報告）。
3. これで商品管理で発注先・メーカーを変えると、店舗別在庫・全店在庫・月別履歴のグループ見出しと並び（発注先→カテゴリ→並び順）に即反映される。確認: 商品管理の編集で発注先を変えた商品が本部画面で新しい発注先グループに移ることを、コード上の導線で説明（ブラウザ確認はログイン不可なら未確認と書く）。

## 共通
- 9月データは触らない。DELETE禁止。パスワード非接触。
- `npm run build` → commit → push → GitHub deployments success まで確認 → 証拠付きで報告。

---
---

# 第4弾（2026-09-09 追加）タスク⑩
第3弾（⑦〜⑨）が main に入った後に着手（`git pull` してから）。

## タスク⑩ 店舗入力画面の検索を全カテゴリ横断に（`src/app/[storeId]/input/page.tsx`）
現状: `products`（選択中カテゴリのみ）を `search` で絞っているため、他カテゴリの商品は見つからない。
`productCatalog`（その店舗の取扱商品を全カテゴリ分保持）が既にある。
やること:
- `search` が空のときは現状どおり選択中カテゴリの商品を表示。
- `search` に文字があるときは `productCatalog` 全体から name/brand で絞り込み（`normalizeSearch` を流用）、
  並びは カテゴリ `sort_order` → その店舗の `store_products.sort_order`（`productCatalog` の取得順を確認し、
  足りなければ取得時に category と sort_order を持たせる）。
- 検索結果の各行に、商品名の上の小字（ブランドの横）でカテゴリ名を表示。
- 数量の +/− 入力は `draftQuantities`（product.id キー）に対して行うので、他カテゴリ商品でも同じハンドラで動くこと。
  `products`（選択中カテゴリの state）だけ更新している箇所があれば、`draftQuantities` 側を正として表示するよう直す。
- 「カテゴリ確認」ボタン・確認ダイアログ・完了処理は変更しない。検索中に確認ボタンを押したときの挙動が現状どおり
  「選択中カテゴリの確認」になることを確認して報告（検索結果に他カテゴリが混ざっていても、確認は選択中カテゴリのみ）。
- 検索中は「全カテゴリから検索中」の一文を表示。

## 共通
- 9月データは触らない。DELETE禁止。パスワード非接触。
- `npm run build` → commit → push → GitHub deployments success まで確認 → 証拠付きで報告。

## タスク⑪ 月別入出庫履歴の商品名を全文表示（`src/app/admin/page.tsx` InventoryHistoryTable）
ユーザー決定: 基本PCで見るので、商品名は省略せず**折り返して全文表示**。
- 商品名セル: `overflow-hidden text-ellipsis whitespace-nowrap max-w-40` を外し、`whitespace-normal break-words leading-snug` に。幅は `w-48 min-w-48`（192px）程度に広げる。ブランド列も `whitespace-normal`。
- 固定列（sticky）はそのまま維持。`left-*` の値を新しい列幅に合わせて再計算する（ずれると重なるので、ブランド/商品名/店舗/月計/業務/店販/個人の各 `left` を幅の累積で正しく設定）。
- 行の高さが変わっても sticky 列の背景が透けないよう、各固定セルに背景色クラスが付いていることを確認。
- スマホ幅で固定列が画面を超えても構わない（PC前提）。

---
---

# 第5弾（2026-09-09 追加）タスク⑫

## タスク⑫ 本部画面のカテゴリボタン・年月切替を「月別の入出庫履歴」の直上に移動（`src/app/admin/page.tsx`）
現状: 画面上部の固定ヘッダーに 年月「‹ 2026年9月 ›」とカテゴリタブ（カラー剤…全店在庫）があるが、
これらが動かすのは一番下の「月別の入出庫履歴」だけ。間に「今日の入力状況」「店舗別在庫」が挟まって分かりにくい。
やること:
- 固定ヘッダーからカテゴリタブの行と 年月切替（‹ › とラベル）と右端の「業務/店販/個人」凡例を取り除く。ヘッダーに残すのは
  「管理」「← 入力」「入出庫」「商品管理」「月締め（9月開始）」のみ。
- `InventoryHistoryTable` の見出し「月別の入出庫履歴」ブロックの中（見出しの下・店舗フィルタ「全店/LABO/nit/elu」の上）に、
  年月切替 → カテゴリタブ の順で配置。`year/month/selectedCat` の state と `prevMonth/nextMonth/setSelectedCat` は
  親（AdminPage）に置いたまま props で渡す（データ取得の仕組みは変えない）。
- 「業務/店販/個人」の凡例は履歴表の下に既に `MOVEMENT_META` の凡例があるので、ヘッダー側のものは削除でよい。
- ヘッダーは引き続き sticky。スマホ幅でも1行に収まる程度に軽くなる想定。
- `npm run build` → commit → push → deployments success まで確認 → 証拠付きで報告。ブラウザ確認は未確認と明記。

---
---

# 第6弾（2026-09-09 追加）タスク⑬

## タスク⑬ 店舗別在庫の表で、商品名と数字の間が空きすぎる（`src/app/admin/page.tsx` HqOverview）
現状: 「全店 店販・Aujua」「全店在庫」「各店舗」タブの表が `w-full` で、PC幅だと商品列が画面幅いっぱいに伸び、
数字（LABO/nit/elu/全店計、現在庫/必要数/不足）が右端に離れて照合しづらい（ユーザーのスクショで確認）。
やること:
- HqOverview 内の3種類の表（retail / shared(全店在庫) / 各店舗）すべてで、表を `w-full` にせず内容幅にする（`w-max` または `w-auto`）。
  商品列は `w-[320px] max-w-[320px]` 程度で固定し、商品名は折り返し（`break-words`）。数字列は `w-16` 程度で商品列のすぐ右に並べる。
  `min-w-[560px]` は外してよい（スマホでは横スクロールの外枠 `overflow-auto` が既にある）。
- 見出し行（発注先・カテゴリの `colSpan` 行）は表幅に合わせて自然に縮む。
- 各店舗タブの必要数タップ編集、全店在庫タブの列構成は変更しない。
- `npm run build` → commit → push → deployments success まで確認 → 証拠付きで報告。ブラウザ確認は未確認と明記。

---
---

# 第7弾（2026-09-09 追加）タスク⑭

## タスク⑭ スマホで検索欄にフォーカスすると画面が拡大される問題
原因: iPhone の Safari は、文字サイズが16px未満の入力欄にフォーカスすると自動で画面を拡大する。
店舗入力画面（`src/app/[storeId]/input/page.tsx`）の検索欄が `text-sm`（14px）、日付欄が `text-xs`（12px）のため発生。
やること:
1. `src/app/layout.tsx` の `viewport` に `maximumScale: 1` を追加（`userScalable` は触らない）。iOS はこれで入力欄フォーカス時の自動拡大をしなくなる。
2. サイト内のすべての `<input>` / `<select>` / `<textarea>` の文字サイズを **16px以上**にする（保険）。
   - 店舗入力: 検索欄 `text-sm` → `text-base`、日付欄 `text-xs` → `text-base`（見た目が大きくなりすぎるなら `text-[16px]` で高さは `py-2` に調整）。
   - 本部側（`src/app/admin/page.tsx`, `admin/operations/page.tsx`, `admin/products/page.tsx`）の検索欄・数量欄・select も `text-sm` 以下なら `text-base` に。
   - `grep -rn "<input\|<select\|<textarea" src` で全件洗い出し、変更した箇所の一覧を報告に載せる。
3. `npm run build` → commit → push → deployments success まで確認 → 証拠付きで報告。実機確認は未確認と明記。
