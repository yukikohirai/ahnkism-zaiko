# 修正プラン 2026-09-09（Fable作成 → Opus実装用）

対象リポジトリ: `/Users/apple/Documents/Codex/2026-08-17/new-chat/work/hpb-inventory`
本番: https://ahnkism-zaiko.vercel.app（GitHub `yukikohirai/ahnkism-zaiko` main への push で自動デプロイ）
Supabase project ref: `tcxjqflprokxewpdoqfw`（MCPの `execute_sql` / `apply_migration` が使える）

## 実装者への絶対ルール
- **憶測で実装しない。** 不明点はユーザー（平井さん・非エンジニア）に日本語で質問する。
- **「やりました」報告は必ず証拠付き。** DBの件数・合計値のクエリ結果、`npm run build` の成功、
  GitHub deployments API の `success` を確認してから報告する。
- 9月分の入力データ（`inventory_movements` 等）は**絶対に消さない・書き換えない**。
- DELETE文は実行しない（必要なら平井さんに Supabase SQL Editor で実行してもらう）。
- パスワードには一切触れない。

## 前提となる現状（Fable調査済み）
- `store_products(store_id, product_id, opening_stock, required_qty, sort_order, is_active, dealer_override)`
  `opening_stock` = 8月末の繰越在庫。現在庫 = `opening_stock + Σ inventory_movements.quantity`（`src/app/admin/page.tsx` `HqOverview.currentStock`）。
- stores: 1=LABO, 2=nit, 3=elu
- categories: 1 カラー剤, 2 ユーショー, 3 パーマ・スパ・その他, 4 トリートメント, 5 備品, 6 店販, 7 店販②, 8 店販オージュア
- 店舗入力画面（`src/app/[storeId]/input/page.tsx`）は `categories` 全件 + その店舗の `store_products` から商品を出す。
  → 新カテゴリを作って `store_products` に3店舗分の行を入れれば、3店舗の入力画面に自動で出る。
- 本部画面 `HqOverview` のタブ: `selectedView === 'retail'`（「全店 店販・Aujua」＝ 店販系カテゴリの店舗別在庫を横並び）と 各店舗タブ。
  「全店在庫」タブは**存在しない**。
- スプシ `/Users/apple/Downloads/【堀江本店】2026 ８月　在庫管理表.xlsx` の `全店在庫` シート:
  列 A=メーカー(結合セル) B=商品名 C=必要数(全部空) D=在庫数(8月末) F=前月繰越 G=各店使用数, 商品1行＋店舗3行(本店/nit/心斎橋)の4行組。
  ユーザー回答: **eluのシートは不要、LABOシートが親。**

---

## タスク① 全店在庫タブ（新規）

### 1-A. データ投入（SQLマイグレーション `supabase/migrations/20260909_shared_stock.sql` を作り、`apply_migration` で適用）
1. `categories` に `(id=9, name='全店在庫', sort_order=9)` を追加（id は明示的に9で良い。既存最大は8）。
2. `products` に下記29件を insert。`category_id=9`, `is_active=true`, `brand=null`, `manufacturer`=下表のメーカー, `dealer=null`, `required_qty=0`, `sort_order`=1..29（表の順）。
   `product_code/purchase_route/image_path/import_key` は null。
3. `store_products` に 29商品 × 3店舗 = 87行 insert。
   - `store_id=1(LABO)`: `opening_stock`=下表の在庫数, `required_qty=0`
   - `store_id=2,3`: `opening_stock=0`, `required_qty=0`
   - 全行 `is_active=true`, `sort_order`=1..29（店舗ごとに同じ順）
   - ※ `モノ` はシートが -1 なので 0 にする（8月取り込み時の「マイナスは0に補正」と同じ方針）。報告時に明記。

| # | メーカー(manufacturer) | 商品名(name) | LABO opening_stock |
|---|---|---|---|
| 1 | (null) | アマテラス6.0銀 | 2 |
| 2 | (null) | アマテラス9.0緑 | 0 |
| 3 | (null) | ルヴニール シャンプー | 8 |
| 4 | (null) | ラポール00 | 8 |
| 5 | (null) | ラポール01 | 8 |
| 6 | (null) | ラポール02 | 8 |
| 7 | センス | 95SENSE シャンプー500ml | 19 |
| 8 | センス | 95SENSE トリートメント500g | 25 |
| 9 | センス | 95SENSE シャンプー詰替 | 0 |
| 10 | センス | 95SENSE トリートメント詰替 | 7 |
| 11 | きくや（ムコタ） | AHNKISMオイル SS | 5 |
| 12 | きくや（ムコタ） | AHNKISMオイル EM | 4 |
| 13 | きくや（ムコタ） | AHNKISMオイル EMレフィル | 0 |
| 14 | きくや（ムコタ） | AHNKISMオイル 緑 | 2 |
| 15 | リファ | ReFa ストレートアイロンプロ | 1 |
| 16 | リファ | ReFa カールアイロンプロ32 | 0 |
| 17 | リファ | ReFa カールアイロンプロ38 | 8 |
| 18 | リファ | パワーストレート（ワイドアイロン） | 0 |
| 19 | リファ | ドライヤー W | 5 |
| 20 | リファ | ドライヤー スマートS＋ | 0 |
| 21 | リファ | ドライヤー SE | 1 |
| 22 | (null) | スピカ | 2 |
| 23 | (null) | モノ | 0 (シートは-1) |
| 24 | (null) | ツガミ | 0 |
| 25 | (null) | リュクス ケラチンブースター250g | 0 |
| 26 | (null) | Cインナーボンド | 0 |
| 27 | (null) | シチリン ピリン | 0 |
| 28 | (null) | Dルネート | 0 |
| 29 | (null) | Fルネート | 0 |

  検証クエリ（報告に結果を貼る）:
  `select count(*) from products where category_id=9;` → 29
  `select store_id, count(*), sum(opening_stock) from store_products sp join products p on p.id=sp.product_id where p.category_id=9 group by 1;` → 1:29/113, 2:29/0, 3:29/0

### 1-B. 本部画面に「全店在庫」タブ（`src/app/admin/page.tsx` の `HqOverview`）
- タブボタンを追加: `selectedView === 'shared'`、ラベル「全店在庫」。位置は「全店 店販・Aujua」の右。
- 対象行: `assignments` のうち `product.category_id` が「全店在庫」カテゴリ（`categories.find(c => c.name === '全店在庫')` で id を引く。ハードコード9は避ける）。
- 商品ごとに1行。列（店舗別在庫の表と同じ見た目・同じ Tailwind クラスを流用）:
  `商品（manufacturer 小字 + name）| 繰越 | LABO | nit | elu | 入荷 | 現在庫 | 必要数`
  - 繰越 = LABO行の `opening_stock`
  - LABO/nit/elu = その店舗の今月 `inventory_movements` のうち **マイナスの合計を正の数で**（使用・販売）。0 は空表示。
  - 入荷 = 3店舗のプラス合計（purchase_order/transfer_in など）
  - 現在庫 = 繰越 + Σ(3店舗すべての movements.quantity)  ← これが「繰越 − 打ち込み分」
  - 必要数 = LABO行の `required_qty`。既存の `saveStoreRequired` を LABO行に対して使い、タップ編集できるようにする。
- `movements` の取得範囲を確認すること: 現状 `loadOverview` が取っている `inventory_movements` の期間・件数制限（`.limit` や当月フィルタ）を読んで、全店在庫29商品×3店舗が漏れないようにする。
- **各店舗タブと「全店 店販・Aujua」タブからは全店在庫カテゴリを除外**する（nit/elu は opening 0 なので現在庫がマイナス表示になって混乱するため）。
- 検索ボックスは全店在庫タブでも効かせる。

### 1-C. 店舗入力画面
- コード変更は原則不要のはず（カテゴリ＋store_products で自動表示）。
  実際に `https://ahnkism-zaiko.vercel.app/1/input` `/2/input` `/3/input` を Playwright で開き、カテゴリ「全店在庫」と29商品が出ることを確認して報告（ログインは honbu アカウント。パスワードは平井さんに入力してもらう）。

---

## タスク② シルク系（LABO 店販 att 10商品）の繰越を修正

原因: 8月末取り込み時、店販シートで att の10行は「必要数(D列)が空欄」で、在庫数(E列)が取り込まれず 0 になっていた。
ユーザー回答: **在庫数を入れて、必要数は 0 のまま。**
9月の入力（product 630 に入荷+18、635 に使用-2）は残っているので `opening_stock` を直せば現在庫は自動で正しくなる。

`store_id=1` の `store_products.opening_stock` を以下に UPDATE（`updated_at=now()` も）:

| product_id | brand/name | opening_stock |
|---|---|---|
| 634 | att シルクシャンプー | 15 |
| 636 | att シルクトリートメント | 18 |
| 633 | att シルクオイル | 27 |
| 638 | att シルクミスト | 20 |
| 632 | att アンプル | 20 |
| 630 | att L | 17 |
| 639 | att ツガミ | 20 |
| 631 | att THE ONE シャンプー | 17 |
| 635 | att シルクシャンプー詰替 | 25 |
| 637 | att シルクトリートメント詰替 | 44 |

（シート E列「在庫数」= 繰越35 + 入 − 出 の8月末値。行72〜91を Fable が目視確認済み）

検証: `select product_id, opening_stock from store_products where store_id=1 and product_id between 630 and 639 order by 1;` の結果を報告に貼る。
本部画面 LABO タブで「シルクシャンプー詰替」が 25−2=23、「L」が 17+18=35 と表示されることを確認。

elu のシルク: ユーザー回答「LABOのシートだけで足りる」→ **elu側は触らない。**

---

## デプロイ手順
1. `npm run build` が通ること
2. `git add -A && git commit -m "..." && git push origin main`
3. `gh api repos/yukikohirai/ahnkism-zaiko/deployments` → 最新の `statuses_url` を見て `success` になったのを確認（`sleep` は使わず、数回ポーリング）
4. 報告: 変更内容 / 検証クエリ結果 / デプロイURL。**確認していないことは「確認していない」と書く。**
